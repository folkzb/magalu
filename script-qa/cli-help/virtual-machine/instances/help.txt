Operations with instances, including create, delete, start, stop, reboot and other actions.

Usage:
  ./cli virtual-machine instances [flags]
  ./cli virtual-machine instances [command]

Commands:
  latest-instances latest-instances
  v0-instances     v0-instances
  v1-instances     v1-instances

Flags:
  -h, --help      help for instances
  -v, --version   version for instances

Global Flags:
      --cli.show-cli-globals   Show all CLI global flags on usage text

Use "./cli virtual-machine instances [command] --help" for more information about a command.


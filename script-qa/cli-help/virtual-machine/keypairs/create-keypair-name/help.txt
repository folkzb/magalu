Create a private and public key

Usage:
  ./cli virtual-machine keypairs create-keypair-name [keypair-name] [flags]
  ./cli virtual-machine keypairs create-keypair-name [command]

Available Commands:
  cli.list-links List all available links for this command

Flags:
  -h, --help                  help for create-keypair-name
      --keypair-name string   keypair_name
  -v, --version               version for create-keypair-name

Global Flags:
      --cli.show-cli-globals   Show all CLI global flags on usage text
      --env enum               Environment to use (one of "pre-prod" or "prod") (default "prod")
      --region enum            Region to reach the service (one of "br-ne-1", "br-ne-2" or "br-se-1") (default "br-ne-1")
      --server-url uri         Manually specify the server to use

Use "./cli virtual-machine keypairs create-keypair-name [command] --help" for more information about a command.

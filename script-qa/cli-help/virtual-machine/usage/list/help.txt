Retrieve usage for specific quota_slug (vcpus, ram, disk & instances)

Usage:
  ./cli virtual-machine usage list [flags]
  ./cli virtual-machine usage list [command]

Available Commands:
  cli.list-links List all available links for this command

Flags:
      --force-authentication   Force authentication by sending the header even if this API doesn't require it
  -h, --help                   help for list
      --quota-slug string      quota_slug
  -v, --version                version for list

Global Flags:
      --cli.show-cli-globals   Show all CLI global flags on usage text
      --env enum               Environment to use (one of "pre-prod" or "prod") (default "prod")
      --region enum            Region to reach the service (one of "br-ne-1", "br-ne-2" or "br-se-1") (default "br-ne-1")
      --server-url uri         Manually specify the server to use

Use "./cli virtual-machine usage list [command] --help" for more information about a command.

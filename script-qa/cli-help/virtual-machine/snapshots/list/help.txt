List all snapshots in the current tenant which is logged in.

#### Notes
- You can use the **expand** argument to get more details from the inner objects
like image and machine types.

Usage:
  ./cli virtual-machine snapshots list [flags]

Flags:
      --control.limit integer     Limit (min: 1)
      --control.offset integer    Offset (min: 0)
      --control.sort string       Sort (pattern: (^[\w-]+:(asc|desc)(,[\w-]+:(asc|desc))*)?$) (default "created_at:desc")
      --expand array(string)     Expand: You can get more detailed info about: ['image', 'type']  (default [])
  -h, --help                     help for list
      --project-type enum        An enumeration. (one of "dbaas", "default", "iamaas", "k8saas" or "mngsvc") (default "default")
  -v, --version                  version for list

Global Flags:
      --cli.show-cli-globals   Show all CLI global flags on usage text
      --env enum               Environment to use (one of "pre-prod" or "prod") (default "prod")
      --region enum            Region to reach the service (one of "br-ne-1", "br-ne-2" or "br-se-1") (default "br-ne-1")
      --server-url uri         Manually specify the server to use

Resizes a database instance.

Usage:
  ./cli dbaas instances resize [instance-id] [flags]

Examples:
  ./cli dbaas instances resize --volume.size=30

Flags:
      --cli.list-links enum[=table]   List all available links for this command (one of "json", "table" or "yaml")
      --cli.watch                     Wait until the operation is completed by calling the 'get' link and waiting until termination. Akin to '! get -w'
      --flavor-id uuid                Flavor Id
  -h, --help                          help for resize
      --instance-id uuid              Database Instance Id: Database Instance Unique Id. (required)
  -v, --version                       version for resize
      --volume object                 Instance Volume Request (single property: size)
                                      Use --volume=help for more details
      --volume.size number            Instance Volume Request: The size of the volume (in GiB). (range: 10 - 50000)
                                      This is the same as '--volume=size:number'.

Global Flags:
      --cli.show-cli-globals   Show all CLI global flags on usage text
      --env enum               Environment to use (one of "pre-prod" or "prod") (default "prod")
      --region enum            Region to reach the service (one of "br-mgl1", "br-ne1" or "br-se1") (default "br-ne1")
      --server-url uri         Manually specify the server to use


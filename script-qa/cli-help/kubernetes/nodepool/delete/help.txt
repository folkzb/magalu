Deleta um node pool em um cluster do Kubernetes

Usage:
  ./cli kubernetes nodepool delete [cluster-id] [id] [flags]

Flags:
      --cluster-id uuid   Identificador no padrão uuid para identificar o cluster (required)
  -h, --help              help for delete
      --id uuid           Identificador no padrão uuid para identificar o nodepool (required)
  -v, --version           version for delete

Global Flags:
      --cli.show-cli-globals   Show all CLI global flags on usage text
      --env enum               Environment to use (one of "pre-prod" or "prod") (default "prod")
      --region enum            Region to reach the service (one of "br-ne1" or "br-se1") (default "br-ne1")
      --server-url uri         Manually specify the server to use


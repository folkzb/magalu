Retrieves the kubeconfig of a Kubernetes cluster by cluster_uuid.

Usage:
  ./cli kubernetes cluster kubeconfig [id] [flags]

Flags:
  -h, --help      help for kubeconfig
      --id uuid   Schema to get the cluster's kubeconfig file. (required)
  -v, --version   version for kubeconfig

Global Flags:
      --cli.show-cli-globals   Show all CLI global flags on usage text
      --env enum               Environment to use (one of "pre-prod" or "prod") (default "prod")
      --region enum            Region to reach the service (one of "br-ne1" or "br-se1") (default "br-ne1")
      --server-url uri         Manually specify the server to use


download an object from a bucket

Usage:
  ./cli object-storage objects download [src] [dst] [flags]

Examples:
  ./cli object-storage objects download --dst="file1.txt" --src="s3://bucket1/file1"

Flags:
      --dst file   Name of the file to be saved
  -h, --help       help for download
      --src uri    Path of the object to be downloaded (required)

Global Flags:
      --cli.show-cli-globals   Show all CLI global flags on usage text
      --region enum            Region to reach the service (one of "br-ne-1" or "br-se-1") (default "br-ne-1")
      --server-url uri         Manually specify the server to use
      --workers integer        Number of routines that spawn to do parallel operations within object_storage (min: 1) (default 5)

